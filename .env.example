# Database (Turso)
# DATABASE_URL - Used by Prisma CLI for migrations/introspection (required)
# For local dev: Use file:./dev.db
# For production: Can keep as file:./dev.db (migrations run locally, then pushed to Turso manually)
DATABASE_URL=file:./dev.db

# TURSO_DATABASE_URL - Used by app runtime via @prisma/adapter-libsql (required)
# For local dev: Use file:./dev.db
# For production: Use your actual Turso database URL
TURSO_DATABASE_URL=libsql://your-database-url.turso.io

# TURSO_AUTH_TOKEN - Auth token for Turso database (required for production)
# For local dev: Use "none" when using file:./dev.db
# For production: Get from `turso db tokens create <database-name>`
TURSO_AUTH_TOKEN=your_turso_auth_token_here

# Feature Flags
# Set to "true" to enable screenshot/image upload feature
ENABLE_UPLOADS=true

# S3 Upload (optional, only needed if ENABLE_UPLOADS=true)
# S3_UPLOAD_KEY=your_s3_key_here
# S3_UPLOAD_SECRET=your_s3_secret_here
# S3_UPLOAD_BUCKET=your_s3_bucket_here
# S3_UPLOAD_REGION=your_s3_region_here

# Optional: Development URL (defaults to localhost:3000)
# NEXT_PUBLIC_DEVELOPMENT_URL=http://localhost:3000

# Note: No POLLINATIONS_AI_TOKEN needed anymore!
# This app uses BYOP (Bring Your Own Pollen) authentication.
# Users connect their Pollinations accounts via OAuth and use their own API keys.
# 
# To use this app:
# 1. Users visit the app and click "Connect with Pollinations"
# 2. They authorize on enter.pollinations.ai
# 3. They're redirected back with an API key
# 4. The key is stored securely in an httpOnly cookie
# 5. All API calls use the user's key (user pays for their own usage)
#
# Get a Pollinations account at: https://enter.pollinations.ai
